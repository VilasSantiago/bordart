---
import Navigation from "../components/Navigation.astro";
import Layout from "../layouts/Layout.astro";
import Divider from "../components/Divider.astro";
import Footer from "../components/Footer.astro";
import TopBtn from "../components/Button.astro";
---

<Layout title="Buzos | BordArt">
    <Navigation />
    <section id="buzos" class="p-6 flex flex-col items-center justify-center min-h-screen bg-gradient-to-b from-white to-yellow-50">
        <div class="max-w-6xl w-full mx-auto">
            <!-- Imagenes -->
            <h1 class="text-4xl md:text-6xl font-bold text-center mb-8 relative">
                <span class="bg-clip-text text-transparent bg-gradient-to-r from-amber-700 to-amber-500">PINES</span>
                <div class="absolute -bottom-3 left-1/2 transform -translate-x-1/2 w-24 h-1 bg-amber-500 rounded-full"></div>
            </h1>
            
            <div id="imagenes" class="max-w-5xl mx-auto my-10 border-amber-200 border-2 rounded-xl shadow-xl overflow-hidden bg-white">
                <img src="/assets/productos/producto-pines-1.jpeg" alt="Remera diseño 1" class="hover:shadow-lg transition-all duration-500 object-contain w-full h-auto">
                <img src="/assets/productos/producto-pines-5.jpeg" alt="Remera diseño 2" class="hover:shadow-lg transition-all duration-500 object-contain w-full h-auto">
                <img src="/assets/productos/producto-pines-3.jpeg" alt="Remera diseño 3" class="hover:shadow-lg transition-all duration-500 object-contain w-full h-auto">
            </div>
            <!-- Opciones de Colores -->
            <div class="mt-12 text-center">
                <h2 class="text-2xl md:text-3xl font-semibold mb-6 inline-block relative">
                    Pines Bordados
                    <div class="absolute -bottom-2 left-0 w-full h-0.5 bg-amber-400 rounded-full"></div>
                </h2>
                <div class="flex justify-center gap-6 mt-8">
                    <p class="text-2xl"> ✨ Detalles que hacen la diferencia. 
                        Ideales para mochilas, riñoneras, cordones y más. Con diseños de todo tipo, siempre hay uno perfecto para vos. 🔥🎒
                    </p>
                </div>
            </div>

            <div class="flex flex-col md:flex-row justify-between items-center max-w-6xl mx-aut0">
                <div class="flex flex-col mx-auto items-center justify-center mt-12 text-center">
                    <a href="/#como-comprar" id="comprar" class="group relative flex items-center gap-2 px-6 py-3 bg-yellow-400 text-black text-lg font-bold rounded-lg shadow-lg transition-all duration-300 hover:bg-yellow-500 hover:shadow-xl">
                        <span class="font-bold text-lg z-10">Ver diseños</span>
                        <img id="cart" src="/assets/mas.svg" alt="" class="w-8 h-8 z-10 transition-all duration-300 group-hover:invert group-hover:brightness-0">
                        <span class="absolute inset-0 bg-yellow-400 z-0"></span> <!-- Span que se animará -->
                    </a>
                </div>
    
                <!-- Como Comprar -->
                <div class="flex flex-col mx-auto items-center justify-center mt-12 text-center">
                    <a href="/#como-comprar" id="comprar" class="group relative flex items-center gap-2 px-6 py-3 bg-yellow-400 text-black text-lg font-bold rounded-lg shadow-lg transition-all duration-300 hover:bg-yellow-500 hover:shadow-xl">
                        <span class="font-bold text-lg z-10">Comprar</span>
                        <img id="cart" src="/assets/cart.svg" alt="" class="w-8 h-8 z-10 transition-all duration-300 group-hover:invert group-hover:brightness-0">
                        <span class="absolute inset-0 bg-yellow-400 z-0"></span> <!-- Span que se animará -->
                    </a>
                </div>
            </div>

            
            
        </div>
    </section>
    <div class="bg-yellow-50">
        <Divider />
    </div>
    <Footer/>
<Layout />

<style>
    #imagenes {
        display: flex;
        width: 100%;
        height: 430px;
    }
    
    #imagenes img {
        width: 0px;
        flex-grow: 1;
        object-fit: cover;
        opacity: 0.8;
        transition: .6s ease;
    }
    
    #imagenes img:hover {
        cursor: crosshair;
        width: 300px;
        opacity: 1;
        filter: contrast(120%);
    }
    
    @media (max-width: 768px) {
        #imagenes {
            height: 300px;
        }
        
        #imagenes img:hover {
            width: 200px;
        }
    }

    @keyframes wipe-in-left {
  from {
    clip-path: inset(0 0 0 100%);
  }
  to {
    clip-path: inset(0 0 0 0);
  }
}

#comprar {
    position: relative;
    overflow: hidden; /* Importante para asegurar que el efecto clip-path se vea */
}

#comprar:hover .absolute {
    animation: wipe-in-left 0.8s cubic-bezier(.25, 1, .30, 1) forwards;
    background-color: rgb(161 98 7 / var(--tw-bg-opacity, 1));
}

#comprar:hover {
    color: white;
}

</style>